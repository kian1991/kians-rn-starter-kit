import { View, Text } from 'react-native';
import React from 'react';

export default function World() {
	return (
		<View className='flex-1 justify-center items-center bg-teal-200 w-full'>
			<Text className='text-2xl font-mono font-bold'>Typewriter.</Text>
		</View>
	);
}
